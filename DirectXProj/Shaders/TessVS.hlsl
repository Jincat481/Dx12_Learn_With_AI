// =============================================================
// TessVS.hlsl - 테셀레이션 지형 정점 셰이더 (스텝 7 / S59)
//
//  테셀레이션을 쓰면 정점 셰이더는 "제어점을 그대로 넘기는" 역할만 한다.
//  실제 정점은 나중에 도메인 셰이더가 만들어 낸다.
// =============================================================
#include "TessCommon.hlsli"

ControlPoint main(ControlPoint input)
{
    return input;
}
